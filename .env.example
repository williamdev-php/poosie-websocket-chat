# Bas-tid i sekunder för radering efter meddelande lästs
# Formeln: BASE_DELETE_TIME + (antal_tecken * TIME_PER_CHARACTER)
BASE_DELETE_TIME_SECONDS=140

# Extra sekunder per tecken i meddelandet
TIME_PER_CHARACTER_SECONDS=0.8

# Max tid ett meddelande kan leva (i sekunder) - för att undvika för långa tider
MAX_MESSAGE_LIFETIME_SECONDS=300

# Krypteringsnyckel (generera en säker nyckel för produktion)
# Generera med: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=s25fsWJNSdiGuLIbIxLhJnN5933iiLANv6qEkY8kSB0=

# Session API Token - ändra detta till något säkert!
SESSION_API_TOKEN=lb2y48k0zDiLL0nBf7dmsZdgG6infCwd

# Server inställningar
HOST=0.0.0.0
PORT=8080

# Cleanup interval - hur ofta systemet kollar efter meddelanden att radera (sekunder)
CLEANUP_INTERVAL_SECONDS=5

# Inaktivitetstid - hur länge innan en användare markeras som inaktiv (sekunder)
INACTIVITY_TIMEOUT_SECONDS=30